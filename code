index=crowdstrike_event_simpleName=RegistryFileWritten
| rex field=TargetFileName "\\Users\\(?<username>[^\\]+)\\(?<remaining>.*)"
| where like(remaining, "%ntuser.dat%")
| table _time, host, username, TargetFileName, remaining
