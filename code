sourcetype="o365:management:activity" Workload=Exchange Operation=Create "\\Calendar"
| spath "Item.ParentFolder.Name" output=FolderName
| spath "Item.ParentFolder.Path" output=FolderPath
| spath "Item.Subject" output=Subject
| search FolderName="Calendar"
| table _time, UserId, MailboxOwnerUPN, Subject, FolderPath, ClientIP, ClientInfoString
