Also, building detections for these events is particularly challenging — the content inside the .ics file, including embedded URLs and phone numbers, is not surfaced in the O365 audit logs, making it impossible to inspect what was written into the calendar entry without additional tooling.
