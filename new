phishing technique that chains WebGL GPU exhaustion, the Fullscreen API, and the Keyboard Lock API to render a personalised fake Windows lockscreen inside a browser tab — harvesting AD credentials without dropping any files, spawning any processes, or triggering any endpoint alerts.
